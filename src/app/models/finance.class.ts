import { Subject } from 'rxjs';

export class Activity {
  amount: string;
  currency: string;
  currencyFormat: string;
  dateTime: Date;
  dateTimeFormat: string;
  description: string;
  status: string;
  type: string;
}
